#include <tunables/global>

profile terminus flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>

  capability chown,
  capability dac_override,
  capability fowner,
  capability fsetid,
  capability kill,
  capability setgid,
  capability setuid,

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  network unix stream,
  network unix dgram,

  signal (send,receive) set=(kill,term,int,hup,cont),

  /bin/** ix,
  /usr/bin/** ix,
  /usr/sbin/** ix,
  /usr/local/bin/** ix,
  /usr/lib/postgresql/** ix,
  /usr/local/bundle/** ix,
  /app/** ix,

  /** mr,

  /data/ rw,
  /data/** rwk,

  /run/ rw,
  /run/** rwk,
  /tmp/ rw,
  /tmp/** rwk,
  /dev/shm/ rw,
  /dev/shm/** rwk,

  /app/log/ rw,
  /app/log/** rwk,
  /app/tmp/ rw,
  /app/tmp/** rwk,
  /app/public/assets/ rw,
  /app/public/assets/** rwk,
  /app/public/fonts/ rw,
  /app/public/fonts/** rwk,
  /app/public/uploads rwkl,
  /app/public/uploads/ rw,
  /app/public/uploads/** rwk,

  /etc/ca-certificates.conf rw,
  /etc/ssl/certs/** rwkl,
  /usr/local/share/ca-certificates/ rw,
  /usr/local/share/ca-certificates/** rwkl,

  owner @{PROC}/@{pid}/** r,
  @{PROC}/cpuinfo r,
  @{PROC}/filesystems r,
  @{PROC}/meminfo r,
  @{PROC}/stat r,
  @{PROC}/sys/kernel/random/uuid r,
  /sys/devices/system/cpu/** r,
}
